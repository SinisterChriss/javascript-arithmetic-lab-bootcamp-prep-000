var number
function add(a, b) {
  number = a + b
  return `${number}`
}
// CORRECT

function subtract(a, b) {
  number = a - b
  return `${number}`
}
// CORRECT

function multiply(a, b) {
  number = a * b
  return `${number}`
}
// CORRECT

function divide(a, b) {
  number = a / b
  return `${number}`
}
// CORRECT

